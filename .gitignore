# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual envs
.venv/
.venv-wsl/
venv/
ENV/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project artifacts
data/raw/
data/processed/
data/cache/
results/*.json
results/run_all_status.txt
results/*.pkl
results/*.npy
results/*.csv
results/figures/
results/runs/
results/samples/
!results/.gitkeep
!results/*.md
!results/env_check.txt

# Model weights / HF cache
models/
*.bin
*.safetensors
*.gguf
.huggingface/
hf_cache/

# Logs
*.log
logs/
wandb/
rocm_install.log
wsl_install.log

# Tooling artifacts created during bootstrap
*.deb

# Vendored external repos (cloned, not committed). Provenance recorded in
# docs/phase2_overview.md. Re-clone with the documented command + commit.
vendor/

# Secrets
.env
.env.local
*.key
hf_token*

# Compiled latex (for Phase 3)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
paper/build/
node_modules/

# per-target resume checkpoints — must survive `git clean -fd`
results/*.jsonl
!results/*.md

# the equivalence-audit sheet, key and labels are EVIDENCE, not derived output.
# results/*.csv above would have silently excluded them, which breaks the
# transparency-in-place-of-inter-annotator-kappa argument the protocol rests on.
!results/equivalence_audit*.csv

# Runtime status surfaces -- rewritten every 15 s by the stop watcher and every 300 s by the
# progress monitor. Tracking them makes the tree permanently dirty and invites an accidental
# commit of whatever a background agent happened to be mid-write on.
dashboard/session_status.json
dashboard/session_status.js
dashboard/progress_status.json
dashboard/progress_samples.jsonl
dashboard/*.stdout
dashboard/*.err
dashboard/stop_watcher*.log
dashboard/overnight_*.log
