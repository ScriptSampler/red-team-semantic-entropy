% Experiments. SKELETON (2026-07-10) --- structure fixed by scripts/null_control.py's
% output; DATA CELLS ARE PLACEHOLDERS ("--"). Fill from results/null_control_*_def.md
% when the definitive run lands. Discipline: no confirmatory claim until n>=80/stratum;
% an interim n=53 pass may be reported as explicitly preliminary.
\label{sec:experiments}

\paragraph{Setup.}
The victim model is Llama-3.1-8B-Instruct in 4-bit quantisation on a single 16\,GB GPU.
Semantic entropy draws $N{=}10$ answers at temperature $1.0$ and clusters them by
bidirectional DeBERTa-large-MNLI entailment; semantic reformulation entropy uses
$R{=}3$ reformulations, $K{=}8$, temperature $0.8$. Our SE replication on TriviaQA
reaches AUROC $0.787$ (published SE figure $0.828$; the gap is the NLI backend, see
Limitations). Targets are drawn by the stratified score-independent sampler; on the
resulting fair pool the clean detector scores AUROC $0.704$ [$0.653$, $0.753$], against
the $1.0$ that score-dependent selection manufactures by construction.

\paragraph{Reading the null control.}
Table~\ref{tab:nullcontrol} places the optimised attack against two null bands---a
same-question \emph{seed-noise} floor and a \emph{benign-paraphrase} floor of $m{=}50$
NLI-passing non-adversarial rephrasings, whose comparison against the attacker's
$\sim$180-candidate ($\sim$181-evaluation) search is budget-matched \emph{analytically} by
the exceedance test below rather than by drawing $m$ equal to
that budget---under each of four clustering relations. ($m$ is
distinct from SRE's samples-per-reformulation $K$.) The
shared-NLI arm is the detector's own clusterer and is the \emph{confounded upper bound}:
it cannot separate a real change in the answer distribution from the NLI model's own
inconsistency, because that same model gates the attack's paraphrases. Exact-match is a
\emph{strict lower bound} that saturates on short factoid answers. The
\texttt{e5-base-unsupervised} embedding relation is NLI-supervision-free but near-chance
on the adversarial, word-preserving case (hard-negative AUROC $0.51$), so it cannot
adjudicate. The LLM-judge (Qwen2.5-7B-Instruct), victim- and NLI-independent and
validated to $0.93$ [$0.90$, $0.96$] ($n{=}300$) on domain-matched gold-alias hard
negatives---a proxy for the deployed clustering decision---is the \textbf{adjudicator}
(for the false-alarm direction, under the disclosed re-scoped gate; Methods).

\paragraph{The claim statistic, and why the obvious ones fail.}
The attack reports the \emph{maximum} over $\sim$181 candidate paraphrases, so any
comparison against individual benign draws is upward-biased by construction: under the null
a max-of-181 sits at $\sim$$99\%$ of an individual-draw distribution, not $50\%$. We
therefore price the search budget into the null analytically. Under the hypothesis that the
optimiser carries no signal, a target's $N$ attack candidates and its $m$ benign draws are
exchangeable, so $F(\text{attack-max}) \sim \mathrm{Beta}(A,1)$ and the number of benign
draws above the attack's maximum is $K \sim \mathrm{BetaBinomial}(m; 1, A)$, with
$\mathbb{E}[K] = m/(A{+}1)$, where $A$ denotes the attacker's candidate count
($A \approx 181$) and is distinct from the per-question sample budget $N{=}10$ used
everywhere else. Fewer exceedances than that is evidence for the attack; the
one-sided $p$-value follows by exact convolution over targets, and the effect size is the
\emph{effective benign budget} $n_{\mathrm{eff}} = m/\bar{K} - 1$, read as ``the search is
worth $n_{\mathrm{eff}}$ random paraphrases''.

Ties are not a technicality here. Semantic entropy is bounded above by $\log N$
(Section~\ref{sec:experiments}, Setup), and benign paraphrases reach that ceiling too, so
the score has an \emph{atom} where the exchangeability null assumes a continuum. We resolve
ties by \textbf{randomised (exchangeable) breaking}: a benign draw tied with the attack's
maximum outranks it with probability $1/(b{+}1)$, where $b$ is the number of attack
candidates also achieving that maximum. Simulation shows why the alternatives are
inadmissible. At full saturation ($q{=}0.05$, $m{=}30$), counting ties strictly gives a null
rejection rate of $0.995$ and counting them against the attack gives power $0.05$ at a
two-fold effect, while the randomised rule retains power $0.71$ there at level $0.093$. At
the pre-registered $m{=}50$, under the empirical $n{=}80$ headroom distribution, the
randomised rule reaches power $0.84$ at level $0.068$. Both inadmissible rules are
reported as diagnostics: their disagreement is a direct indicator that ceiling saturation,
rather than attack strength, is driving a comparison. Note that $b$ must be \emph{measured}
rather than estimated, since it is precisely where attack strength appears once the ceiling
pins the maximum's value; our optimiser records it per target.

\begin{table}[t]
\centering
\caption{Null-controlled attack effect, SE / false-alarm direction, per clustering
relation. Bands are mean intended move (nats) and are descriptive only: in the pilot the
benign floor is negative in the false-alarm direction and the seed~$<$~benign ordering does
not always hold, so the band ordering is not the test. The test is the randomised-tie
exceedance statistic; the saturation rate is reported beside it because it governs how much
the nats bands can be trusted, and the two inadmissible tie rules are shown so that any
disagreement between them is visible. Every cell is measured on the \emph{false-alarm attack
pool}---the $n{\geq}80$ score-independently selected correct-answer targets the optimiser was
run on---and not on the fair pool used to characterise the clean detector (Section
\ref{sec:methods}); the two populations differ and no number is carried between them.
\emph{Placeholder cells pending the definitive run}; confirmatory $n{\geq}80$, $m{=}50$.}
\label{tab:nullcontrol}
\begin{tabular}{lcccc}
\toprule
 & Shared NLI & Exact-match & Embedding & LLM-judge \\
 & (confounded) & (strict LB) & (e5, cannot adj.) & (adjudicator) \\
\midrule
Seed-noise floor (nats)        & -- & -- & -- & -- \\
Benign floor (nats)            & -- & -- & -- & -- \\
Attack (nats)                  & -- & -- & -- & -- \\
\midrule
\textbf{Exceedance test $p$ (randomised ties)} & \textbf{--} & \textbf{--} & \textbf{--} & \textbf{--} \\
\textbf{Effective benign budget $n_{\mathrm{eff}}$} & \textbf{--} & \textbf{--} & \textbf{--} & \textbf{--} \\
\midrule
Saturation rate (\% at $\log N$) & -- & -- & -- & -- \\
Diagnostic: strict-tie $p$     & -- & -- & -- & -- \\
Diagnostic: conservative-tie $p$ & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Decision rule (single, pre-registered, with disclosed deviations).}
Reframe~(a)---``the attack targets the detector''---is claimed iff, at $n{\geq}80$ with
$m{=}50$ benign paraphrases per target, the adjudicator clusterer's randomised-tie
exceedance test rejects at the $5\%$ level in the direction of the attack. That design was
fixed in advance at a simulated power of $0.84$ against a two-fold effect and $0.99$ against
a three-fold one, chosen over a cheaper $m{=}30$ (power $0.67$) because our own
selection-on-noise measurement indicates the true effect is modest and the design must be
able to see a small one. If the test does not reject, (a) is not claimed, and the
NLI/exact-match bracket together with the judge's point estimate is reported as the honest
bounded result.

Four deviations from the original pre-registration are disclosed as outcome-triggered
rather than folded silently into ``the pre-registered rule''. The adjudicator moved from the
sentence embedder to the LLM-judge, triggered by the embedder's $0.51$ hard-negative AUROC.
The benign floor moved from individual draws to a budget-corrected comparison, triggered by
the winner's-curse mismatch surfaced in adversarial review. The budget correction then moved
from brute-force matching to the analytic exceedance null, triggered by the matched design
pricing out at roughly nine GPU-days. And the tie rule moved from conservative to randomised
breaking, triggered by the simulation above showing the conservative rule has no power under
the ceiling. Each trigger is objective, each was recorded before the corresponding data was
seen, and the full sequence is given here rather than in an appendix precisely because a
reader is entitled to check that the rule was not tuned to the result.

\paragraph{Transfer and defense.}
These results are pending: the SE$\leftrightarrow$SRE transfer table and the
reformulation-averaging defense curve fill from the same campaign; both are reported net of the benign floor and
read as corroborating rather than independent evidence (the two detectors share the
DeBERTa backbone).
