% Discussion. See paper/skeleton.md section 6.
% Why it works: SE's equivalence clustering is itself NLI-bounded, so a
% paraphrase that shifts the model's sampling distribution without crossing the
% NLI boundary moves entropy without changing meaning. False-alarm exploits SE's
% own over-fragmentation of a correct answer phrased many ways.
\textit{Draft pending main results.}
