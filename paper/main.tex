% Red-Teaming Semantic Entropy --- arXiv preprint scaffold.
% Generic arXiv-friendly preamble (swap in neurips/iclr style before submission).
\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
% figures/ lives at the repo root; accept a build launched from either the root or paper/.
\graphicspath{{../figures/}{figures/}}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage[numbers]{natbib}
\usepackage[margin=1in]{geometry}

% Working title --- avoids collision with "Adversarial Paraphrasing" (2506.07001).
% PROPOSED Option-B title (protocol-led); the attack-led original is kept below, commented.
\title{Crying Wolf, Carefully: What It Takes to Evaluate Paraphrase Attacks on
Sampling-Based Hallucination Detectors}
% \title{Crying Wolf: Meaning-Preserving Paraphrases Break Sampling-Based
% Hallucination Detectors in Both Directions}   % attack-led (Option A) original
% affiliation intentionally omitted for the preprint; fill before any external posting.
\author{ScriptSampler}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
% 321 words / ~2020 characters as counted on 2026-08-19 (215 words at the last commit;
% ~200 at the critic gate of 2026-08-13). It grew twice on 2026-08-19: first when the N=40
% sentences were rewritten from an existence claim into a measured operating point, then
% when the floor's interval was withdrawn and replaced by the empty-atom bound, which costs
% words because it has to say what 2.0% is instead of quoting a range. arXiv's abstract
% field takes 1920 characters, so this is OVER by about 100 and must be trimmed before
% posting -- but not by dropping an interval or a qualifier, and not by reinstating an
% interval on the N=40 floor, which is withdrawn on purpose (results/n40_floor_estimator_
% ruling.md). Count it with the same rule before believing any number in this comment.
% Everything
% cut from here is still stated where it belongs: the three-estimator scoping and the
% Eq.(4)/Eq.(5) transfer argument in Methods, the N=20 pilot in Discussion, the judge
% validation in Methods, the interval on every rate in Discussion and Conclusion.
Sampling-based hallucination detectors such as semantic entropy are deployed on the claim
that the uncertainty they report is invariant to phrasing. We set out to
break it with meaning-preserving paraphrases and found the binding constraint falls on the
operator, not the attacker. The \emph{discrete} estimator we study weights meaning clusters
by sample counts, making it the entropy of a partition of $N$: at the standard $N{=}10$ it
takes only $39$ values, with an atom at the maximum. Every threshold above that maximum flags
nothing, so while the atom carries mass the lowest false-alarm rate a firing threshold can
have \emph{is} the chance that a clean correct answer yields $N$ mutually
distinct meanings---$10.5\%$ [$9.0$, $12.2$] over $1424$ clean correct answers. At that
standard $N{=}10$ an operator who specifies a $5\%$ false-alarm budget cannot have one,
however well the score ranks; the qualifier is load-bearing. Running the fair pool's $200$
correct answers out to $N{=}40$ empties the atom---none reaches $\ln 40$ ($0/200$, at most
$1.9\%$)---and the obstruction goes with it. What $200$ answers cannot then do is locate the
floor: the cheapest alarm they can exhibit costs $2.0\%$, the resolution of the pool rather
than a property of the detector, and we give it no interval.
The threshold that honours a $5\%$ budget achieves $5.0\%$ [$2.7$, $9.0$] on those same $200$
answers, which we report as a measured operating point and not as a demonstration that one at
or below $5\%$ exists. Measuring an attack here
needs stronger controls than this literature's random-perturbation baseline: re-scoring each
selected paraphrase on an independent sample retains only $44\%$ [$23\%$, $64\%$] of the
apparent effect, so roughly half of an uncorrected result is selection on sampling noise.
Our protocol makes the question answerable: score-independent target selection, an
answer-invariance success criterion, a null priced against the attacker's search budget, and
an independent LLM-judge equivalence oracle. The confirmatory attack evaluation is under
way; we claim no attack effect here.
\end{abstract}

\section{Introduction}
\input{sections/introduction}

\section{Related Work}
\input{sections/related_work}

\section{Methods}
\input{sections/methods}

\section{Experiments}
\input{sections/experiments}

\section{Discussion}
\input{sections/discussion}

\section{Limitations}
\input{sections/limitations}

\section{Conclusion}
\input{sections/conclusion}

\bibliographystyle{plainnat}
\bibliography{related_work}

\end{document}
