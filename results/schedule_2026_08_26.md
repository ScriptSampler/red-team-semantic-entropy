# Remaining-work schedule, rebuilt from measured rates — 2026-08-26

**This file supersedes `results/schedule_2026_08_19.md`.** That file is kept as a record and
should not be quoted for any number below; five of its figures are corrected here, and one of
its corrections is that its own headline measurement was 7% short.

**36 days to the 2026-10-01 arXiv target** (moved from 2026-09-15). Written read-only against
the repo: nothing here launched GPU work, ran anything on the GPU, edited `paper/`, `scripts/`,
`tests/` or `docs/`, created a file of any STOP spelling in either watched location, or touched
the live run. The run in `Ubuntu-24.04` was inspected once with read-only `ps` and left alone.

## The rule this file obeys, unchanged

Every number carries a tag:

- **MEASURED** — an artifact and the measurement are named.
- **DERIVED** — arithmetic from measured inputs, shown inline.
- **ASSUMED** — the assumption is named, and the number is not treated as evidence.

An untagged number is the failure mode this project has already paid for: a `~60 GPU-h` figure
that was a model, never a measurement, and that a scheduling decision rested on. Where nothing
can be measured, the row says **not measurable yet** and says what would make it measurable. No
plausible figure is substituted for a missing one.

---

## 0. The answer in five lines

1. **The extension bought exactly nothing.** The human budget went from 28.0 h to **35.0 h**
   (+7.0 h, +4 sessions). The zero-activity gap that occurred over the same period consumed
   **4 sessions = 7.0 h**. Net change since 2026-08-19: **+0.0 h.** Both halves MEASURED; see §3.2.
2. **GPU-hours are further from binding than they were.** Blocking GPU work is now
   **16.7–18.7 GPU-h** (was 23.2–27.5) against **707.5 h** of usable calendar — a **2.6%** duty
   cycle. The null control's rate is no longer bracketed; it is measured over 20 intervals.
3. **The binding budget is human hours, and it binds on rows nobody has costed.** Priced blocking
   demand is **~7.9 h of 35.0**. Four rows remain unpriceable and three of them block.
4. **The headline risk is the duty cycle, and it got worse, not better.** A second zero-activity
   gap has occurred: **2026-08-19 08:38:22 → 2026-08-26 20:23:33, 7.49 days, not one file
   modified anywhere in the tree** (MEASURED two ways, corroborated by git). Over the 15.80 days
   since 2026-08-11, **78.1% of the calendar was inside a zero-activity gap.**
5. **The window can absorb about 3 more such gaps. At the measured recurrence rate it will
   contain about 4.6.** That is the number this schedule turns on, and it is the only one on this
   page that can kill the paper.

---

## 1. The owed-item table

| # | Item | GPU-h | tag | Human-h | tag | Blocks preprint? |
|---|---|---|---|---|---|---|
| 1 | **Null control to 80/80** (48 targets left) | **15.95–17.99** | **DERIVED from MEASURED rate** | ~0 once launched; 1 launch/session | ASSUMED | **BLOCKS** |
| 2 | **Judge conditions (ii)+(iii), machine half** | **0.74** | DERIVED from MEASURED units | ~1 (analysis) | ASSUMED | **BLOCKS** (Abstract/Methods scope) |
| 3 | **Confirmatory n≥80 independent-clusterer run** | **0.00 — not a separate job** | MEASURED, re-verified §1.3 | 0 | — | **BLOCKS**, discharged by #1 |
| 4 | **Question-equivalence audit, round 1** (123 pairs) | 0 | — | **2.05–3.08** | ASSUMED pace | **optional** — the blocking flag was stale (§1.4) |
| 5 | **Question-equivalence audit, round 2** (37 pairs) | 0 | — | **0.62–0.93** | ASSUMED pace | optional (cut #6) |
| 6 | **Table 1 fill-in + Experiments/Discussion rewrite** from #1's output | 0 | — | **not measurable yet** | — | **BLOCKS** |
| 7 | **Null-objective validity gate** | **8.3–18.9** (central 13.5) | DERIVED from MEASURED unit | ~1 | ASSUMED | optional — pre-committed fallback is free |
| 8 | **Reformulation-averaging defense** | **4.02** (2.01 FA-only) | DERIVED from the script's own model | ~1–2 | ASSUMED | optional |
| 9 | **Direct N=10 drift control** | **0.14** (40) / **0.71–1.43** (200/400) | DERIVED from MEASURED unit | ~0.5 | ASSUMED | optional |
| 10 | **Judge condition (ii), human half** (new 160-pair sheet) | 0 | — | **3–4** + sheet build | ASSUMED | optional — repo already recommends cutting |
| 11 | **SRE cells + SE↔SRE transfer table** | **not measurable yet** (floor ≥26) | — | not measurable yet | — | optional |
| 12 | **Paper edits forced by `replay_control.md`** | 0 | — | **0.36 priced + unpriced residue** | see §1.6 | **BLOCKS** — mostly spent |
| 13 | **Punchlist still-open** M6, M8, M9, M12, mi3, mi5, mi6 | 0 | — | **not measurable yet** | — | mixed |
| 14 | **LaTeX toolchain + compile** | 0 | — | **0.25–1.0** | **MEASURED install + DERIVED residual** | **BLOCKS** |
| 15 | **NEW: trim the Abstract to arXiv's 1920-char field** | 0 | — | **not measurable yet** (small) | — | **BLOCKS** — hard submission-form reject |

Two rows changed status since 08-19 and one row is new. Item 4 is **no longer blocking** (§1.4),
item 14 is **priced** (§1.7), and item 15 did not exist on the previous list (§1.8).

### 1.1 Item 1 — the null control. The rate is now measured, not bracketed.

**State, MEASURED** (`dashboard/progress_status.json`, generated 2026-08-26 20:28:33 by the live
`scripts/progress_monitor.py`): `done: 32`, `total: 80`, `remaining: 48`, `carried_over: 32`,
`produced_this_session: 0`, `torn_lines: 0`. The checkpoint's mtime is **2026-08-19 08:18:38**,
age **648,594 s = 7.5 days**. Watcher healthy, 2 GPU processes, last wrote 14 s before the sample.

**The run relaunched at 2026-08-26 20:23:41 and has produced nothing yet.** That is expected — it
is replaying 32 checkpoint records and loading models; `dashboard/overnight_20260814.log` shows
the 32 `(checkpoint)` replay lines and then generation-config warnings. It is the same job:
read-only `ps -eo pid,ppid,etime,stat,args` inside `Ubuntu-24.04` returns PID 529,
`null_control.py --tag _defb --only false_alarm --K 50 --n_seeds 3 --judge_model
Qwen/Qwen2.5-7B-Instruct --judge_batched --judge_batch_size 6 --dump_diag results/diag_defb.json
--checkpoint auto`, elapsed 13m, under wrapper PID 521, with the stop watcher (387) and
supervisor (499) alive.

**The monitor refuses to project, and it is right to.** `projection.available: false`, because
`"only 0 target(s) timed this session; need >= 2 before a rate means anything"`. Every one of the
21 intervals in its `gaps` array is excluded with `"starts before the session resumed (resume
gap)"`. **I do not overrule that refusal: I make no claim about tonight's rate.**

But the exclusion is relative to *this* session's start, not a statement that the intervals are
meaningless — and `dashboard/progress_samples.jsonl` retains the whole 2026-08-19 session. That
session is over, complete, and directly measurable. **This is the measurement the 08-19 file said
would become available the moment target 13 landed. It landed, and 19 more after it.**

**MEASURED rate**, from the 22 distinct `(n, mtime)` completion events in
`progress_samples.jsonl`, discarding the one genuine resume gap (`11→12`, 484,744 s):

```
targets 12 → 32 : 2026-08-19 01:39:46 → 08:18:38
                = 23,931.69 s over 20 intervals
                = 1196.58 s/target          <- MEASURED, 20 intervals
```

| basis | s/target | tag |
|---|---|---|
| **This measurement, 20 intervals** | **1196.58** | **MEASURED** |
| Prior run, `null_control_cost_options.md` §1, 10 intervals | 1191.00 | MEASURED |
| Monitor's own last-5 at 07:45 / all-18 at 07:45 | 1209 / 1185 | MEASURED |
| Full-armed targets only (17 of 20, excluding 391.7 / 796.0 / 944.5 s short arms) | 1282.32 | MEASURED, subset |
| `c_arms = 24.0` fit, every remaining target full-armed | 1349.00 | DERIVED |

**The prior 1191 s/target is confirmed to within 0.5%.** Three of 20 targets short-armed (15%),
against 2 of 11 (18%) in the prior run — consistent. Per-target spread 391.7–1404.1 s.

**Remaining cost, DERIVED:**

```
48 × 1196.58 s  =  57,436 s  =  15.95 GPU-h    (measured mix, best estimate)
48 × 1282.32 s  =  61,551 s  =  17.10 GPU-h    (no remaining target short-arms)
48 × 1349.00 s  =  64,752 s  =  17.99 GPU-h    (modelled full 50-draw upper bound)
```

**⇒ 15.95–17.99 GPU-h, central 16.0.** The 08-19 file's 22.5–26.8 is superseded: 20 targets of
that estimate have since been produced, and the rate it bracketed is now a point measurement.

**Relaunch overhead, MEASURED, and small.** The 08-19 session started 01:16:10 and landed target
12 at 01:39:46 — 1,416 s against a steady-state 1,196.58 s, so model load plus checkpoint replay
cost **~220 s**. At one relaunch per session that is noise against 16 GPU-h.

**The number that actually governs item 1 is not GPU-hours — it is launches.** The 2026-08-19
launch ran **7.37 h** (01:16:10 → 08:38:22, terminated by STOP, not by completion) and produced
**20 targets**. So:

```
15.95 GPU-h / 7.37 h per launch  =  2.16 launches of the observed length
```

**Two more overnight launches finish the null control.** Three null-control launches have
occurred since 2026-08-11 (by 08-13, on 08-19, and tonight) over 15.80 days — **one per 5.3 days**
(MEASURED). At that cadence 2.2 more launches is **~11.6 days**, landing about 2026-09-07. That
fits in 36 days with three weeks in hand, and it is the fit that matters, not the 2.6% duty cycle.

### 1.2 Item 2 — judge conditions (ii) and (iii). Unchanged, and the trap is still live.

`0.74 GPU-h` stands, DERIVED from MEASURED inputs (`operational_number_audit.md` §2.7):

```
160 judge clusterings × 11.13 s  +  80 victim generations × 11.21 s  =  2,678 s = 0.74 GPU-h
```

**Do not quote `3.11 GPU-h`** — `judge_owed_conditions.md` §6.2 flags itself as a model built on
two refuted anchors at the wrong batch size. **The "free" option remains closed and the hazard is
now worse, not better:** `null_control.py:338` keys checkpoint reuse on the whole cfg dict, so
adding `--dump_judge_detail` now would stop **32** completed targets matching and silently restart
at target 1 — a 10.6 GPU-h loss at the measured rate, to save 0.74. **Pay the 0.74.**

### 1.3 Item 3 — re-verified. Still not a separate job.

The 08-19 file's biggest correction still holds, and I checked it against the live process rather
than against that file. `limitations.tex:145` reports the independent-clusterer result as
*"preliminary evidence, pending the confirmatory $n{\geq}80$ run"*; `conclusion.tex:67` names the
run as the one *"under this judge"*; `discussion.tex:430` calls it *"now under way"*;
`methods.tex:366` and `main.tex:66` agree. The process running right now carries
`--judge_model Qwen/Qwen2.5-7B-Instruct --judge_batched` at n=80. **That process is that run.**

**Incremental cost: zero.** Discharged by item 1 and by nothing else.

### 1.4 Item 4 — the blocking flag was stale, and this is a correction to the 08-19 file

The 08-19 file flagged the question-equivalence audit **BLOCKS (title word *meaning-preserving*)**.
**That is wrong, and it was wrong when written.** MEASURED at `paper/main.tex:18`, the live title is:

> Crying Wolf, Carefully: What It Takes to Evaluate Paraphrase Attacks on Sampling-Based
> Hallucination Detectors

The attack-led title containing *Meaning-Preserving* is at `:20`, **commented out**. The
2026-08-19 morning review caught this independently (decision 3: *"The schedule's 'BLOCKING (title
word meaning-preserving)' flag is stale"*). **The preprint can ship without this audit.** Running
it converts a disclosed limitation into a measurement; not running it costs a sentence.

**State, MEASURED — and this is the finding that should worry the user most after §3:**
`results/equivalence_audit.csv` is **123 rows, 0 annotated**; `equivalence_audit_round2.csv` is
**37 rows, 0 annotated**. Both files last modified **2026-08-13 02:45**, thirteen days ago. The
08-19 review's first line was *"Today, and only today: decision 3 — human equivalence audit,
round 1."* **Seven days later, zero pairs have been annotated.** The pace remains ASSUMED
(`equivalence_audit_protocol.md` §4, 60–90 s/pair, cap 40/session) and has still never been
observed on this sheet.

```
round 1:  123 × 60–90 s  =  7,380–11,070 s  =  2.05–3.08 h   (4 sessions)
round 2:   37 × 60–90 s  =  2,220– 3,330 s  =  0.62–0.93 h   (1 session)
```

**The ≥7-day washout (protocol §7a) is the only hard calendar constraint in the project** and it
still binds — but the extension has bought it real slack. R1 finishing by 2026-09-11 leaves the
washout comfortable; see §5.

### 1.5 Items 7, 8, 9, 10, 11 — unchanged, and re-checked as still owed

No artifact has appeared for any of them since 08-19 (nothing anywhere in the tree was modified
between 08-19 08:38 and 08-26 20:23, so this is a measurement, not an assumption). Their costs
carry forward from `operational_number_audit.md` §§2.5–2.7 and `schedule_2026_08_19.md` §§1.5–1.8
unchanged: **7 = 8.3–18.9 GPU-h** (central 13.5, strictly after item 1, needs `diag_defb.json`),
**8 = 4.02**, **9 = 0.14/0.71/1.43** depending on a sizing that is still undecided,
**10 = 3–4 h human plus an unpriced sheet build**, **11 = not measurable yet, floor ≥26 GPU-h**.

### 1.6 Item 12 — largely spent. This is the biggest reduction on the list.

The 08-19 file could only say **not measurable yet**. Four overnight rounds ran on 2026-08-19 and
spent most of it. Verified from `results/morning_review_2026_08_19.md` and `docs/critique_log.md`
entries 36–38, and by grepping the eight `.tex` files:

**Landed (MEASURED — the files carry the edits):**

- Decisions **1, 6, 8, 9 CLOSED.** Entry 38: round 4 ran the coverage simulation and **withdrew
  both candidate intervals** for the measured N=40 floor — Wilson covers in 53.7% of 40,000
  simulated pools, the question bootstrap in **0.00%** — because the estimand dissolves once the
  ceiling atom empties. `results/n40_floor_estimator_ruling.md` is the ruling; the paper now
  prints no interval on that row and rests the Abstract on the obstruction being gone.
- **`paper/` came out clean.** Two independent verifiers, one re-deriving the whole paper from
  `results/n_scaling_ckpt.jsonl` importing no project script, found no fabricated or tidied value.
- Test suite **2 failed / 1014 passed / 15 skipped**, both survivors one pre-existing
  do-not-edit file reported twice.

**Still owed, and priced where it can be:**

| residue | cost | tag |
|---|---|---|
| **Decision 5** — two claims in the reject direction, `discussion.tex:225-226` ("therefore itself falsified", asserted from two point estimates whose intervals overlap their bands) and `:311` (a null accepted from a non-significant χ²) | **0.33 h** | ASSUMED (morning review: "~20 minutes together") |
| **Decision 7** — the `24.0` quarantine collision: ratify "leave it out, no exemption" | **0.03 h** | ASSUMED |
| **Decision 10** — seven residue sites (`START_HERE`, `replay_control.md:344` and `:203`, `test_derived_paper_quantities.py:54`, two unpinned legs, a figure sidecar) | **~30 min of AGENT time, ~0 user time** | stated by the review; being closed by another agent this session |
| `n_scaling_grid.md:22` N=20 row + `:25-26` unqualified absence claim | not measurable yet (CPU-only, cheap) | — |

**Entry 38's rule is the one that governs the remaining paper work,** and it should be read
before any further correction is applied:

> A correction is applied to exactly the representations you enumerated. […] enumerate the FORMS
> the claim takes, not the files it sits in.

Six of round 4's seven misses would have survived a single owner with unlimited scope. Budgeting
paper corrections by file count will under-price them.

### 1.7 Item 14 — the LaTeX toolchain is now priced, and it is small

`results/latex_build_risk.md` (2026-08-19) closes the 08-19 file's largest unpriced blocker.

**MEASURED:** no LaTeX exists on **either** side. Windows — `pdflatex`, `latexmk`, `xelatex`,
`lualatex`, `tectonic`, `miktex`, `tex`, `bibtex`, `biber`, `pdftex`, `kpsewhich` all absent, and
no install directory present. WSL `Ubuntu-24.04` — same nine binaries absent, `dpkg -l` returns
**zero** texlive packages (all 20 matches are the substring "tex" inside "text"), no
`/usr/share/texlive`. **The zero-cost escape hatch does not exist.**

**MEASURED install cost**, via `apt-get install --print-uris` / `-s`, which installs nothing:
`texlive-latex-base texlive-latex-recommended texlive-fonts-recommended` = **49.1 MB download,
21 packages, 144 MB on disk, ~5 min**. Not the multi-gigabyte job the "do not start an unattended
install" constraint was written against.

**MEASURED source state:** `scripts/check_latex_source.py` exits 0 — **0 blocking errors** across
8 files / 1514 lines. All 6 `\input`s resolve, 27/27 `\cite` keys resolve, 6 labels / 6 refs clean
both directions, braces and math and environments balanced across the whole tree, no custom
macros, **no non-ASCII anywhere** (the commonest silent killer of a first build), float internals
in the right order. The 61 warnings are all false positives or `%` inside `.bib` `annote` fields
that `plainnat` never emits.

```
install (MEASURED)  ~5 min
known source fixes  0
residual only a real compiler can surface (DERIVED from the 0-error static result)  0-45 min
--------------------------------------------------------------------
first compiled PDF: 15-60 min, one-time
```

**⇒ 0.25–1.0 human-h.** The 08-19 file's framing — *"a paper that has never been compiled can be
arbitrarily far from building"* — is over-priced on the consequence and correct on the toolchain.
Two things a first compile must produce: `main.tex:12-13` loads `hyperref` before `natbib`
(likely cosmetic, one-line swap), and **arXiv does not run BibTeX, so the generated `main.bbl` is
a required submission artifact**, not a build intermediate.

**Nine `.tex` files have now been edited across four rounds and none of it has been compiled.**
That is a gap in evidence rather than a defect, and it grows with every round.

### 1.8 Item 15 — NEW, blocking, and it is a hard reject at the submission form

**MEASURED**, by parsing `\begin{abstract}…\end{abstract}` out of `paper/main.tex` with comment
lines stripped: **2,023 characters** of plain text (2,109 raw LaTeX), **320 words**. arXiv's
abstract field takes **1,920**. **The abstract is 103 characters over and would be rejected today.**

This reproduces `main.tex`'s own header comment ("~2020 characters […] OVER by about 100") to
within 3 characters, independently. The 08-19 morning review lists it in its top three. It is not
on the 08-19 schedule at all.

The comment also constrains the fix, and the constraint is load-bearing: trim **not** by dropping
an interval or a qualifier, and **not** by reinstating an interval on the N=40 floor, which is
withdrawn on purpose. Cost **not measurable yet** — but it is 103 characters, and it is the
cheapest blocking item on the list.

---

## 2. Budgets, and which one binds

### 2.1 GPU

| | hours |
|---|---|
| Raw calendar, 2026-08-26 20:30 → 2026-10-01 | 36.15 d = **867.5 h** |
| **Usable** calendar (last useful landing 2026-09-25 08:00, leaving the final 4 sessions to write up) | **707.5 h** |
| Blocking GPU demand (items 1 + 2) | **16.7–18.7 GPU-h** |
| Everything on the table (1, 2, 7, 8, 9) | **29.2–43.1 GPU-h** |
| SRE (item 11) if attempted | **≥26, unpriced** |

Blocking demand is **2.4–2.6%** of usable calendar (was 5.0–6.0%). The full priced list is
**4.1–6.1%**.

**Contention warning, unchanged and MEASURED.** All rate anchors for item 1 are span-over-count
on a shared device, so contention that occurred is already inside them. For items 7, 8 and 9 it is
not: `operational_number_audit.md` §2.5 measures the `_defb` FA cell at **13.0 GPU-h of compute
inside a 29.7 h wall span (2.28×)**, and warns that *"any sentence of the form 'X GPU-h, so it
fits in a night' is silently asserting a 1.0× ratio."* Running anything alongside the null control
invokes that 2.28× on both.

### 2.2 Human

**35.0 h scheduled** (DERIVED from the recorded cadence Mon/Wed/Fri 08:00–10:00 + Sunday
10:00–11:00, enumerated against the calendar): **20 sessions** from Fri 2026-08-28 through Wed
2026-09-30 — **15 × 2 h + 5 × 1 h**. Today's Wed 08-26 08:00 slot has already passed unused.

*(Method check: the same enumerator reproduces the 08-19 file's 16 sessions / 28.0 h exactly.)*

| demand | hours | tag | blocks? |
|---|---|---|---|
| Per-session run-state read and relaunch | 0.25 × 20 = **5.0** | ASSUMED | yes, operationally |
| Judge machine-half analysis (item 2) | **~1** | ASSUMED | yes |
| LaTeX install + first compile (item 14) | **0.25–1.0** | MEASURED install + DERIVED residual | yes |
| Decision 5 — soften two claims (item 12) | **0.33** | ASSUMED | yes |
| Decision 7 — ratify the quarantine (item 12) | **0.03** | ASSUMED | yes |
| Abstract trim to 1920 chars (item 15) | not measurable yet | — | **yes** |
| Table 1 + Experiments/Discussion rewrite (item 6) | **not measurable yet** | — | **yes** |
| Punchlist M6, M8, M9, M12, mi3, mi5, mi6 (item 13) | **not measurable yet** | — | mixed |
| arXiv submission mechanics (`.bbl`, metadata, form) | **not measurable yet** | — | **yes** |
| — *optional* — Question audit R1 + R2 (items 4, 5) | 2.7–4.0 | ASSUMED, never observed | no (§1.4) |
| — *optional* — Judge (ii) human half (item 10) | 3–4 + sheet build | ASSUMED | no |

**Priced blocking core: 6.6–7.9 h of 35.0. Full priced list including both optional audits:
12.5–15.9 h of 35.0.**

Four rows are unpriceable from any artifact and **three of the four block the preprint** — down
from three of four on 08-19 only because item 14 got priced and item 15 got added. **The human
budget binds, and it still binds on the rows nobody has ever costed.**

### 2.3 Verdict — and what the extra 16 days changed

**GPU: not binding, and less binding than before.** 18.7 GPU-h against 707.5 h usable is a factor
of 38. The extra 16 days did not change this answer, because it was never close.

**Human: binding, and the extra 16 days changed nothing at all.**

```
08-19 budget:  16 sessions,  28.0 h   (2026-08-19 -> 2026-09-15)
08-26 budget:  20 sessions,  35.0 h   (2026-08-26 -> 2026-10-01)
              --------------------
extension:     +4 sessions,  +7.0 h
gap 08-19->08-26 consumed:
               -4 sessions,  -7.0 h   (Fri 08-21, Sun 08-23, Mon 08-24, Wed 08-26)
              ====================
net change:     0 sessions,   0.0 h
```

Both halves MEASURED — the sessions from the recorded cadence, the gap from §3. **The sixteen
extra days bought back exactly the week that was lost, and no more.** Anyone reasoning "we now
have 36 days instead of 27, so there is room" is reasoning from raw calendar; the schedulable
resource is unchanged from a week ago.

**The failure mode is not "we ran out of GPU-hours", and it is not "the deadline was too close."
It is a third seven-day silence.**

---

## 3. The duty cycle. This is the headline risk, and the measurement supports it.

### 3.1 A second gap happened, and it is longer than the first

**MEASURED — full `os.stat` walk of the tree, skipping `.git`, `node_modules`, `.venv*`,
`.claude`. Run twice, once additionally excluding `__pycache__` and `.pytest_cache` (316 files)
and once including them (560 files). Both walks agree.**

```
GAP 2:  2026-08-19 08:38:22  ->  2026-08-26 20:23:33
        7.490 days  =  179.75 hours
        NOT ONE FILE MODIFIED ANYWHERE IN THE TREE
        start anchor: results/n_scaling_plan.md
        end anchor:   dashboard/stop_watcher.log
```

**The seven-day figure in the brief is confirmed, and it is 7.49 days.**

**Corroborated independently by git, which the 08-19 file did not check.** The last commit is
`e58915f` at **2026-08-19 08:41:00** ("close-out: partial round five, interrupted by STOP at
08:38"). There is **no commit after it**, and `git status --porcelain` is **empty** — so there is
not even uncommitted work sitting in the tree. Git gives the same gap: **7.488 days**.

**And a correction to the 08-19 file's own headline.** Re-measuring gap 1 today:

```
GAP 1:  2026-08-14 04:45:58  ->  2026-08-19 01:14:19   =  4.853 d   (os.stat walk)
        2026-08-14 05:09:07  ->  2026-08-19 01:18:36   =  4.840 d   (git commit timestamps)
        the 08-19 file reported                           4.53 d
```

Two independent methods agree on **~4.84 days**, against the 4.53 d that file published. Its start
anchor — a file with mtime 2026-08-14 12:19:24 — no longer exists in the tree at that timestamp;
nothing anywhere in the repo, including `__pycache__` and `.pytest_cache`, now carries an mtime
between 2026-08-14 05:09 and 2026-08-19 01:14. **The first gap was 7% longer than reported.**

**Method caveat, stated because it cuts one way.** mtime-based gap measurement is lossy: a file
rewritten later erases the evidence that it was written earlier, which can only make a measured
gap *longer* than the truth, never shorter. Gap 1's drift from 4.53 to 4.84 d is that mechanism
visible. **Gap 2 is not exposed to it yet** — nothing modified during it has been rewritten since,
and git corroborates to within four minutes. Treat gap 2's 7.49 d as exact and gap 1's 4.84 d as
an upper bound.

### 3.2 What fraction of the calendar is silence

**MEASURED spans, DERIVED fraction:**

```
window            2026-08-11 01:21:40 -> 2026-08-26 20:30  =  15.797 days
inside a gap      4.853 (gap 1)  +  7.490 (gap 2)          =  12.343 days
                                                            ---------------
                                                             78.1% silent
```

**78.1% of the last sixteen days was inside a zero-activity gap.**

The GPU side tells the same story. Identifiable GPU-busy time in that window is 29.7 (FA cell) +
24.3 (hide cell) + 3.3 (null control 1–11) + 5.8 (overnight queue) + **7.37 (null control 12–32,
MEASURED tonight from the samples file)** + 0.12 (tonight so far) = **70.6 h of 379.1 h = 18.6%
duty cycle**, down from the **33.1%** the 08-19 file measured over its window.

### 3.3 How many more gaps the 36-day window absorbs

**GPU side, DERIVED:**

```
blocking only:      707.5 - 18.7 = 688.8 h slack  /  179.75 h per gap  =  3.83 gaps
everything priced:  707.5 - 43.1 = 664.4 h slack  /  179.75 h per gap  =  3.70 gaps
```

**Human side, DERIVED** — and this is the one that binds, because a gap costs sessions, not just
calendar. The gap just measured contained **4 sessions = 7.0 h = 20.0% of the entire remaining
human budget**:

```
priced blocking core:  (35.0 - 7.9) / 7.0  =  3.88 gaps
full priced list:      (35.0 - 15.9) / 7.0 =  2.73 gaps
```

**⇒ The window absorbs about 3 more seven-day gaps — call it 2.7 if both audits are kept, 3.8 if
they are cut. The two budgets have converged: on 08-19 the GPU absorbed 3.9 gaps against a human
budget that was already over-subscribed; today they are within one gap of each other.**

**Against that, the measured recurrence rate.** Two gaps in 15.797 days is **one gap per 7.90
days** (DERIVED from 2 MEASURED gaps — **n = 2**, and this is the thinnest inference on the page;
it is stated because the alternative is silence, not because two points are a rate). Over the
remaining 36.15 days:

```
36.15 / 7.90  =  4.58 gaps expected
```

**The window is on course for about 4.6 gaps and can absorb about 3.** That is the finding. Every
cost estimate on this page can be exactly right and the paper still does not ship.

**Three things make this sharper than the 08-19 version of the same warning:**

1. **It is no longer a single observation.** One gap is an anecdote; two consecutive gaps
   totalling 78.1% of the calendar is a pattern, and the second was **55% longer** than the first.
2. **The 08-19 file named this risk and the risk then materialised, in the same week, without
   changing anything.** Its §5 item 12 read: *"A 4.53-day zero-activity gap is MEASURED and
   immediately precedes today. Nothing in the repo models how often that recurs."* It recurred
   within 24 hours of that sentence being written, at 1.55× the length.
3. **The single item the 08-19 file said was calendar-locked and must start "in the first
   available session" has zero rows annotated seven days later.** The audit sheets are byte-for-byte
   what they were on 2026-08-13. Slack measured in sessions is not slack if the sessions do not
   happen — the morning review's own phrasing, and it has now been demonstrated twice.

**What would falsify this, and it is worth wanting.** Two consecutive weeks in which the Mon/Wed/Fri
sessions produce any file modification at all would put the gap rate at ≤1 per 14 days, and the
window absorbs 2.6 of those comfortably. The measurement is cheap: it is the same `os.stat` walk,
and it takes seconds.

---

## 4. The cut list, ordered

Take them from the top. Each line states what the paper loses. **The first three are now cheaper
to take than they were on 08-19, because item 4 turned out not to block.**

**Cut 1 — SRE cells and the SE↔SRE transfer table.** Saves **≥26 GPU-h (unpriced)** plus unpriced
analysis. *Cost:* `experiments.tex:186`'s "Transfer and defense" paragraph loses its transfer half
and the paper stops promising anything across detectors — but the claim was never made, only
promised, and the batch gate in critique_log 34 already named SRE as a cut.

**Cut 2 — judge condition (ii), human half (item 10).** Saves **3–4 h of the scarcest resource**
plus an unpriced sheet build. *Cost:* the oracle label's own error rate stays unbounded and
Limitations gains one disclosed assumption. `judge_owed_conditions.md` §8 already wrote this cut:
*"If one must be dropped, drop this one."*

**Cut 3 — the reformulation-averaging defense (item 8).** Saves **4.02 GPU-h and 1–2 h**. *Cost:*
nothing currently claimed — `conclusion.tex` frames it as *"an open question we frame but do not
yet settle"*; only `experiments.tex:186`'s pending-curve sentence has to go.

**Cut 4 — the question-equivalence audit entirely, both rounds (items 4 and 5).** Saves **2.7–4.0 h
and removes the ≥7-day washout from the critical path altogether.** *Cost:* the paper keeps a
disclosed limitation instead of gaining a measurement, and the single-annotator agreement statistic
never exists. **This cut moved up the list, and it moved up on evidence, not preference:** the
title no longer contains *meaning-preserving*, so nothing in the paper depends on it (§1.4). It is
now a strict quality improvement rather than a load-bearing claim. **Take this cut if R1 has not
started by 2026-09-11** — that is the last date from which R1 (4 sessions) plus the washout plus
R2 still fits before the write-up sessions.

**Cut 5 — the null-objective gate (item 7).** Saves **8.3–18.9 GPU-h**. *Cost:* pre-committed and
already written down in `null_objective_ablation_plan.md` §7 under **NOT RUN** — lead with the free
prefix statistic (`trajectory_best_obj` is on disk for all 80 targets), demote the exceedance test
to a labelled companion, disclose that 91.8% of the tie multiplicity is provably multi-hop.
**Trigger: take this cut if the null control has not written `diag_defb.json` by 2026-09-15.**
(Was 2026-09-01; moved with the deadline and with item 1's smaller remainder.) The plan warns that
*"'no time' will not be a defensible reason"* — take it on the trigger, not on drift.

**Cut 6 — the direct N=10 drift control (item 9).** Saves 0.14–1.43 GPU-h. *Cost:* the direct-N=10
row's provenance stays unexplained. Small, because `replay_control.md` established that both claims
resting on it die either way.

**Cut 7 — null control from n=80 to n=60.** Saves **~6.3 GPU-h** (19 targets × 1196.58 s).
*Cost:* power against a 2× effect falls **0.77 → 0.56**, the n≥80 pre-commitment breaks, and
because the interim has already been read the paper must disclose that an interim was seen **and**
that n was cut on compute grounds. For a null result the power qualifier *is* the result. **Do not
take this cut.** It was already poor value at 68 targets remaining; at 48 remaining and 16 GPU-h
total it buys 6.3 GPU-h out of a 707 h window — it is not a saving, it is a self-inflicted
limitation.

### What must not be cut

**The null control itself.** Design G (prefix-only, zero new GPU) forfeits the benign arm that
every fallback is defined against — **including the free prefix statistic that cut 5 falls back
to.** Cut 5 and this cut are not independent; taking both leaves nothing. On the exceedance test:
`null_control_cost_options.md` §3 establishes that the smallest p the test can *ever* return is a
hard function of n (0.068 > 0.05 at n=11 — a floor, not a power statement). **At n=32 that
particular floor no longer binds, so the argument against stopping here is the pre-commitment and
the power curve, not an arithmetic impossibility.** Stating it the other way round would be the
kind of stale-anchor reuse this file exists to prevent. The largest saving any cheaper design now
offers is ~6.3 GPU-h out of a 707 h window. There is nothing to buy here.

**Item 15, the abstract trim.** It is 103 characters and a hard reject at the submission form.

---

## 5. Critical path, and what overlaps the unattended GPU work

**Two chains. The GPU chain is no longer the long one, and after cut 4 neither is the audit.**

```
null control  16.0 GPU-h  =  ~2.2 launches of the observed 7.37 h length
   -> results/diag_defb.json
        +-> null-objective gate (8.3-18.9 GPU-h)   [cut 5]
        +-> Table 1 fill-in -> Experiments/Discussion/Conclusion final text
        +-> the confirmatory independent-clusterer verdict (Limitations, Methods, Abstract)
   -> device idle -> judge (ii)+(iii) machine half (0.74 GPU-h) -> Abstract scope closed
                  -> LaTeX install (5 min) -> first compile -> main.bbl
```

```
R1 audit (4 sessions, ~3 h human)   [OPTIONAL - cut 4]
   -> WASHOUT >= 7 days  [calendar, cannot be compressed]
      -> R2 (1 session) -> round-3 adjudication -> scoring -> Limitations text
```

**Runs unattended, needs no human once launched:** items 1, 2, 7, 8, 9. **Between launches the
human is free, and this is the whole trick — it is also the thing that has not been exploited for
two consecutive weeks.** Over the 7.49-day gap the GPU was idle *and* the human was idle; the
overlap that makes this schedule work was available the entire time and unused.

**Needs a human and no GPU — overlap these with item 1:** items 4, 5, 6 (drafting), 12, 13, 15,
and the LaTeX install (item 14 is CPU and disk, not GPU — the only hazard is the dpkg lock inside
the distro running the job, and `apt` contention against a GPU job is I/O, not compute).

**Genuinely serialised:**

- item 7 strictly after item 1 (needs `diag_defb.json`).
- item 2 wants an **idle** device; running it alongside the null control invokes the measured
  2.28× wall inflation on both.
- item 6 strictly after item 1.
- item 15 (abstract trim) strictly after item 6 — trimming before Table 1's numbers land means
  trimming twice.
- R2 strictly ≥7 days after R1.
- **Decision 10 before any further agent runs** — `docs/START_HERE_overnight.md` currently
  instructs the next agent to move the repo *toward* the estimator round 4 withdrew at 0.00%
  coverage. An obedient agent would undo the night's work and report progress. Being closed this
  session by another agent; ~30 min of agent time, no user time.

**Sequencing:**

| session | do this |
|---|---|
| **tonight (already running)** | Null control relaunched 20:23. Leave it. Expect ~20 targets by ~04:00, i.e. **52/80**. |
| Fri 08-28 | Read `progress_status.json` — with ≥2 targets timed this session the monitor will project for the first time. **Decide cut 4 (audit in or out).** If in, start R1 (40 pairs) and **time the first 40** — at 120 s/pair R1 is 4.1 h, not 2.7, and that is when you re-decide. Relaunch the run. |
| Sun 08-30 | Decision 5 (0.33 h) + decision 7 (0.03 h). Short session, exactly fits. |
| Mon 08-31 | **LaTeX: install (5 min) and get the first PDF.** Converts the last blocking unpriced-cost row into a number. Keep `main.bbl`. |
| Wed 09-02 | R1 pairs 41–80, or punchlist M12/mi5/mi6 (mechanical) if cut 4 taken. Relaunch the run. |
| Fri 09-04 → Sun 09-06 | R1 pairs 81–123. **Null control expected complete around here** (2.2 launches from 08-26 at the measured 5.3-day launch cadence ≈ 09-07). |
| Mon 09-07 | Null control done → launch judge machine half (0.74 GPU-h) on the idle device. **R1 complete; washout clock starts.** |
| Wed 09-09 → Sun 09-13 | **Table 1 fill-in + Experiments/Discussion rewrite (item 6)** — the largest unpriced blocking row, and the first session it becomes possible. Launch the null-objective gate if `diag_defb.json` exists. |
| Mon 09-14 → Wed 09-16 | **Washout elapsed. R2** (1 session), then round-3 adjudication + scoring + Limitations text. |
| Fri 09-18 → Wed 09-23 | Punchlist M6, M8, M9, mi3. M8/M9 are new Related Work prose, not edits — treat as the long pole here. |
| Fri 09-25 | **Last useful GPU landing.** Nothing new starts on the device after this. |
| Sun 09-27 → Wed 09-30 | **Abstract trim to 1920 (item 15). Final compile, `main.bbl`, `.bib` `%TODO` verification, errata #4 page cites, arXiv submission.** |

**Slack in this plan: R1 has six sessions of slack before the washout stops fitting** (was five at
27 days). Item 6 has three sessions. **The plan survives one seven-day gap. It does not survive
two, and it does not survive one placed on top of the item-6 window in mid-September.**

---

## 6. Unpriced risk

Costs I could **not** establish from any artifact. Four of these block the preprint.

1. **Table 1 fill-in and the Experiments/Discussion rewrite** (item 6). `experiments.tex:129-145`
   is an all-placeholder table — every data cell `--`, caption says so. It will compile, as a table
   of dashes. **Blocking.** Not measurable until item 1's output exists. **This is now the largest
   unpriced blocking row, having inherited the position from the LaTeX toolchain.**
2. **arXiv submission mechanics.** Metadata, categories, licence, the `main.bbl`, the first upload's
   own build on arXiv's TeX Live. Distinct from item 14 and never estimated. **Blocking.**
3. **The Abstract trim** (item 15). 103 characters, with a stated constraint on how it may not be
   done. Small, but **blocking**, and unpriced.
4. **Punchlist still-open items** (item 13): M6, M8, M9, M12, mi3, mi5, mi6. **M8 and M9 are
   Related Work strands — new prose, not edits**, and `docs/paper_review_punchlist.md:17` has
   carried them open since 2026-07-11, six weeks.
5. **Judge (ii) human-half sheet construction** — retarget `prepare_equivalence_audit.py`, rewrite
   protocol §1 and §3 for the new 160-pair design. Removed by cut 2; priced here in case cut 2 is
   refused.
6. **Round-3 adjudication pass** on round-2 label flips. Count unknown until R2 runs.
7. **SRE throughput** (item 11). `≥26 GPU-h` is a floor derived from the SE analogue, not a
   measurement. A 6-target probe (~1 GPU-h) would price it.
8. **Critic-gate turnaround.** `docs/critic_charter.md` gates each checkpoint and requires clearing
   BLOCKs before proceeding. Never costed in wall-clock or human-hours.
9. **Errata #4** — the N=10 citation claim (*"the budget Kuhn, Farquhar and Kossen all use"*) is
   unverified and needs three page cites before the Abstract sentence ships.
10. **Two `%TODO` verification markers in `related_work.bib`** (lines 142, 196, 204), inside
    `annote` fields, flagging claims attributed to two references that were not confirmable from
    the abstract. Invisible to the build; the `.bib` header flags itself VERIFY-BEFORE-SUBMIT.
    **Live for submission.**
11. **Two of three generated figures are never included.** `fig1_ceiling.pdf` and
    `fig2_censoring.pdf` exist and no `\includegraphics` references them; the paper ships exactly
    two figures (`fig_achievable_roc`, `fig_floor_budget`). Given that the surviving headline is the
    granularity/false-alarm-floor finding, this looks like an oversight rather than a decision.
12. **Errata #6** — `fig_achievable_roc*` and `fig1_ceiling*` are N=10-only and want an N=20/N=40
    companion panel, or a caption line confining them.
13. **Negative control for the nesting check**: `verify_nesting` asserts the prefix property but
    nothing demonstrates it failing at another seed, so the assertion is unproven as non-vacuous.
    Cheap, CPU-only, never scoped.
14. **Item 9's sizing is still undecided** and the two readings differ by 10× (40 targets vs 400).
    A 40-target spot check cannot separate drift from subsetting on a floor statistic measured at
    n=200. Decide before running, not after.
15. **The duty cycle.** Still the largest unpriced quantity on this page — but it is no longer
    unmeasured. **Two gaps, 4.84 d and 7.49 d, 78.1% of the calendar, one per 7.90 days.** What
    remains unpriced is only *why*, and nothing in the repo models it. See §3.

**Explicitly conditional, and not counted as owed:** re-fixing τ on a disjoint split is owed *only
if* the crossing test is promoted from disclosed secondary diagnostic to a claim statistic. Do not
promote it.

---

## 7. Where every number came from

| figure | tag | artifact / method |
|---|---|---|
| 32/80 done, 48 remaining, 0 produced this session | MEASURED | `dashboard/progress_status.json` @ 2026-08-26 20:28:33 |
| checkpoint age 648,594 s (7.5 d) | MEASURED | same, `checkpoint_age_s` |
| **1196.58 s/target, 20 intervals** | **MEASURED** | `dashboard/progress_samples.jsonl`, 22 completion events, resume gap excluded |
| 1191 s/target (prior run, 10 intervals) | MEASURED | `null_control_cost_options.md` §1 |
| 1209 / 1185 s/target | MEASURED | monitor's last-5 / all-18 at 2026-08-19 07:45 |
| 1282.32 s/target (17 full-armed) | MEASURED, subset | samples file, 3 short arms excluded |
| 1349 s/target | DERIVED | `c_arms = 24.0 s` fit |
| ~220 s relaunch overhead | MEASURED | 1416 s to target 12 vs 1196.58 steady state |
| 7.37 h / 20 targets per launch | MEASURED | samples file, 2026-08-19 01:16:10 → 08:38:22 |
| live process is the judge-armed n=80 run | MEASURED | read-only `ps` in `Ubuntu-24.04`, PID 529 |
| 12.87 s / N=10 SE eval; 11.13 s judge increment; 11.21 s N=10 generation | MEASURED / DERIVED | `operational_number_audit.md` §§2.1, 2.3, 2.7 |
| 13.0 GPU-h compute inside 29.7 h wall (2.28×) | MEASURED | audit §2.5, `_defb` FA cell |
| 0.74 GPU-h judge conditions | DERIVED | audit §2.7 |
| 8.3–18.9 GPU-h null-objective gate | DERIVED | audit §2.6 |
| 4.02 GPU-h defense | DERIVED | `scripts/wk_defense.py` cost model + measured SE eval |
| 123 / 37 pairs, **0 annotated**, files unchanged since 2026-08-13 02:45 | MEASURED | CSV parse of both sheets + `os.stat` |
| 60–90 s per pair | **ASSUMED** | `equivalence_audit_protocol.md` §4, never observed |
| ≥7-day washout | protocol constraint | `equivalence_audit_protocol.md` §7a |
| live title contains no "meaning-preserving" | MEASURED | `paper/main.tex:18`; attack-led variant commented at `:20` |
| **abstract 2,023 chars vs arXiv 1,920 — over by 103** | **MEASURED** | parse of `\begin{abstract}` in `main.tex`, comments stripped |
| no LaTeX on Windows **or** WSL; 0 texlive packages | MEASURED | `latex_build_risk.md` §2 |
| 49.1 MB / 21 packages / 144 MB / ~5 min install | MEASURED | `apt-get --print-uris` / `-s`, installs nothing |
| 0 blocking LaTeX errors, 8 files / 1514 lines | MEASURED | `scripts/check_latex_source.py` exit 0 |
| 15–60 min to first PDF | DERIVED from the above | `latex_build_risk.md` §1 |
| Wilson 53.7% / bootstrap 0.00% coverage, 40,000 pools | MEASURED (not by me) | `results/n40_floor_estimator_ruling.md`, via critique 38 |
| **gap 2: 7.490 d, zero files modified** | **MEASURED, two walks** | `os.stat` walk, 316 and 560 files |
| gap 2: 7.488 d, zero commits, clean tree | MEASURED | `git log`, `git reflog`, `git status --porcelain` |
| gap 1 re-measured: 4.853 d (mtime) / 4.840 d (git) | MEASURED | vs the 4.53 d published on 08-19 |
| 78.1% of 15.797 d silent | DERIVED from MEASURED spans | (4.853 + 7.490) / 15.797 |
| 18.6% GPU duty cycle over 379.1 h | DERIVED from MEASURED spans | audit §2.5 + §1.1 above |
| **one gap per 7.90 d; 4.58 expected in 36.15 d** | **DERIVED from 2 MEASURED gaps — n = 2** | §3.3; the thinnest inference here, flagged as such |
| 20 sessions / 35.0 h | DERIVED | recorded cadence enumerated against the calendar; reproduces the 08-19 file's 16 / 28.0 exactly |
| gap cost 4 sessions / 7.0 h; extension gave 4 sessions / 7.0 h | DERIVED from MEASURED | §2.3 |
| 707.5 h usable calendar | DERIVED | now → 2026-09-25 08:00, final 4 sessions reserved |
| 3.70–3.88 gaps absorbable | DERIVED | §3.3 |

**Dead anchors that appear nowhere in this file, deliberately:** `~60 GPU-h`, `~67 GPU-h`,
`228 GPU-h`, `55 s/clustering`, `73.9 s/eval`, `2.8 s` as an SE-eval unit, `6.1 s` as a cheap-arm
unit, `67 s` as a judge-clustering unit, `3.11 GPU-h` for the judge conditions, `3.2–5.7 GPU-h`
for the null-objective gate, `1416.75 s/target` (a single startup-inflated interval the 08-19 file
correctly refused to treat as a rate), `22.5–26.8 GPU-h` for item 1 (superseded — 20 of those
targets are done), `4.53 d` for gap 1 (re-measured at 4.84), and **`BLOCKS (title word
meaning-preserving)`** for item 4 (the title does not contain the word). If one of them reappears
in a schedule, that schedule was not rebuilt — it was copied.
