# Red-Teaming Semantic Entropy — Python deps
# Pin hard before tagging env-ready (per Week 1 deliverable).
# PyTorch installed separately — see README for ROCm wheel index URL.

# Core ML
transformers==4.46.3
datasets==3.1.0
accelerate==1.1.1
huggingface-hub==0.26.2
safetensors==0.4.5
tokenizers==0.20.3

# Quantisation (CUDA-first; ROCm fork installed separately if needed)
bitsandbytes==0.44.1

# NLI / semantic equivalence (Week 3+)
sentencepiece==0.2.0
protobuf==5.28.3

# Numerics + eval
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.5.2
pandas==2.2.3

# Utilities
tqdm==4.67.0
python-dotenv==1.0.1
pyyaml==6.0.2
click==8.1.7

# Plotting (Week 9+ figures)
matplotlib==3.9.2
seaborn==0.13.2

# Dev / notebooks
jupyterlab==4.3.1
ipykernel==6.29.5

# Testing
pytest==8.3.3
